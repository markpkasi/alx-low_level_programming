#include <stdio.h>
#include "main."

/**
 * main - print the name of the program
 * @argc: count arguments
 * @argv: arguments
 * Return: 0 (success)
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
