#include "main.h"
#include <stdio.h>

/**
 * mul - a program tha multiplies two integers
 * @a: first number
 * @b: second number
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
